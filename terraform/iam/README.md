# IAM module (stub)

To implement:
- Scoped IAM role + instance profile for EC2 (least privilege — only what the app needs)
- Scoped IAM role for the Lambda function (see ../lambda-sns)
- Document explicitly why AdministratorAccess is NOT used (see docs/Security-Controls.md)
