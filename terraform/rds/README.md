# RDS module (stub)

To implement:
- `aws_db_instance` (Free Tier eligible class, see variables.tf)
- `publicly_accessible = false`
- Placed in the private subnet via a DB subnet group
- Attached to `sg-rds-db` only
